<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="BaseBorderStyle" TargetType="Border">
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="CornerRadius" Value="20"/>
        <Setter Property="Padding" Value="10"/>
    </Style>

    <Style x:Key="EmptyBlueBorderStyle" TargetType="Border" BasedOn="{StaticResource BaseBorderStyle}">
        <Setter Property="Background" Value="{StaticResource TransparentColor}"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="BorderBrush" Value="{StaticResource BlueColor}"/>
    </Style>

    <Style x:Key="WhiteBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource WhiteColor}"/>
        <Setter Property="CornerRadius" Value="20"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Margin" Value="40"/>
        <Setter Property="Padding" Value="20"/>
    </Style>

    <Style x:Key="VerticalGrayLineBorderStyle" TargetType="Border">
        <Setter Property="Width" Value="2"/>
        <Setter Property="Background" Value="{StaticResource MediumGrayColor}"/>
        <Setter Property="Margin" Value="0, 0, 0, 10"/>
    </Style>

</ResourceDictionary>